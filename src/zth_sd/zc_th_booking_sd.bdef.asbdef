projection;
strict ( 2 );
use draft;

define behavior for ZC_TH_BOOKING_SD alias Booking
{
  use create;
  use update;
  use delete;

  use action ConfirmBooking;
  use action CancelBooking;
  use action CompletePayment;

  use action Activate;
  use action Resume;
  use action Edit;
  use action Discard;
  use action Prepare;

}